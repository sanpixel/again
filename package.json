{
  "name": "again",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/index.js",
    "dev": "cd backend && npm run dev"
  }
}
